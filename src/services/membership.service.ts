import { MembershipPlan } from '@/types';

/**
 * Membership — future feature.
 * Returns empty array. No active membership system.
 * UI page renders with no plans until membership is implemented.
 */
export function getMembershipPlans(): MembershipPlan[] {
  return [];
}
